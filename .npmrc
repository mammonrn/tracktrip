# Turn EBADENGINE from a warning into a hard install failure.
#
# google-auth-library v11 quietly required Node >=22 while the VPS runs
# Node 20; npm only warned, so the mismatch was easy to miss. With this on,
# `npm ci` fails outright instead, on this machine and in CI alike.
engine-strict=true
